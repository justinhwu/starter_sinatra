<h1> <%= "#{@instructor.first_name} #{@instructor.last_name}" %> </h1>

<b><label for="instructor_courses">Courses Taught: </label></b><br>
  <% @instructor.courses.each do |key| %>
  <a href='/courses/<%= key.id%>'><%= "#{key.subject} #{key.course_number}"%><br>
    </a>
  <%end%>
<form action="/instructors/<%=@instructor.id%>/edit" method="get">
    <input type="submit" value="Edit this instructor"/>
</form>

<form action="/instructors" method="get">
    <input type="submit" value="Go back to the list of instructors"/>
</form>
